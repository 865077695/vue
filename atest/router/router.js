/** * Created by zzq on 2017/6/2. */// 1.定义路由组件//可以从其他文件import进来const Foo = {template:'<div>foo</div>'}const Bar = {template:'<div>bar</div>'}//2.定义路由/** 每个路由应该映射一个组件。* 其中"component"可以是通过Vue.extend()创建的组件构造器* 或者，只是一个组件配置对象* 我们先不考虑嵌套路由* */const routes = [  {path:'/foo',component:Foo},  {path:'/bar',component:Bar}]// 3.创建路由实例，传入路由配置/** 还有很多参数可以传入，暂时就这么简单吧* */const router = new VueRouter({  routes//缩写，相当于routes:routes})// 4.创建和挂载根实例/** 记得要通过router配置参数注入路由* 从而让整个应用都有路由功能*/const app = new Vue({  router}).$mount('#app')